# Core
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# Classical TS
statsmodels>=0.14

# DL stack (use the version Colab Pro provides; pin loosely)
torch>=2.1
einops>=0.7

# Hugging Face / foundation forecasters
transformers>=4.40
accelerate>=0.30
chronos-forecasting>=1.4
# Note: use `pip install 'timesfm[torch]'` to skip the JAX/paxml dep tree,
# which pins to lingvo with no Python 3.12 wheels. The plain extra-less install
# will fail on Colab (Python 3.12).
timesfm[torch]>=1.2   # Google TimesFM foundation model
uni2ts>=1.2           # Salesforce Moirai foundation model

# RL utilities (we write our own PPO loop; keeping deps minimal)
gymnasium>=0.29

# Eval / stats
properscoring>=0.1
matplotlib>=3.7

# Config / logging
hydra-core>=1.3
omegaconf>=2.3
tqdm>=4.66

# Test
pytest>=7.4
